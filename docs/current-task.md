# 현재 작업

기준일: 2026-07-22

Notion 기준:

- `아임웹 제작 대행 운영 웹앱 — 3주 MVP 구현 계획`
- `이번 주 작업 보드 (7/21~7/26)`

현재 저장소 상태:

- `/Users/youngjinson/developer/imweb`에는 `.nvmrc`만 존재한다.
- `.nvmrc`는 Node.js `20.19.0`을 지정한다.
- 아직 Next.js 프로젝트 파일, Supabase 설정, 문서 구조가 없다.

## 2026-07-22 작업

### 프론트엔드

작업: 프로젝트 초기 세팅·폴더 구조 구성

상태: 진행 중

목표:

- Next.js, TypeScript, Tailwind, shadcn/ui 기반 프로젝트를 만든다.
- 공개 사이트와 관리자 영역의 라우트/레이아웃을 분리한다.
- FSD 기준으로 `src/app`, `src/widgets`, `src/features`, `src/entities`, `src/shared` 구조를 만든다.
- Next.js Pages Router와 충돌하지 않도록 `src/pages`는 만들지 않는다.
- 실제 기능 구현은 최소화하고 이후 세션이 작업할 수 있는 기반만 만든다.

완료 기준:

- 로컬 개발 서버가 실행된다.
- 공개 라우트와 `/admin` 라우트가 `src/app`에서 구조적으로 분리되어 있다.
- UI, 도메인, 기능, 공통 코드가 FSD 레이어 규칙에 따라 배치된다.
- `npm run lint`, `npm run type-check`, `npm run build` 스크립트가 준비되어 있다.

### 백엔드

작업: Supabase 연결·핵심 DB 스키마 작성

상태: 진행 중

목표:

- Supabase 클라이언트 구조와 환경변수 이름을 정의한다.
- 관리자, 문의, 고객, 프로젝트, 결제/입금, 포트폴리오, AI 생성 기록 테이블 초안을 만든다.
- 공개 문의 등록과 관리자 조회의 권한 경계를 RLS로 설계한다.
- API 계약과 서버 유틸은 FSD 경계에 맞춰 `src/shared`, `src/entities`, `src/features`, `src/app/api`에 배치할 기준을 세운다.
- 비밀값은 `.env.local`에만 두고 문서/코드에 직접 기록하지 않는다.

완료 기준:

- 마이그레이션 또는 SQL 문서로 핵심 테이블 정의가 남아 있다.
- 공개/관리자 권한 정책 초안이 있다.
- 프론트엔드가 사용할 타입 또는 API 계약 초안이 있다.

## 2026-07-23 작업

### 프론트엔드

작업: 공개 홈페이지 기본 화면 구현

상태: 시작 전

목표:

- 홈, 서비스, 제작 과정, 가격, FAQ 섹션을 기능 중심으로 반응형 구현한다.
- 포트폴리오와 제작 문의 폼으로 이동할 수 있는 동선을 만든다.
- 디자인은 과한 랜딩보다 실제 대행 서비스 신뢰와 전환에 집중한다.

완료 기준:

- 모바일/데스크톱에서 첫 화면과 주요 섹션이 깨지지 않는다.
- 문의 CTA가 명확하다.
- 가격/FAQ는 이후 관리자 관리나 Supabase 연동으로 교체 가능한 구조다.

### 백엔드

작업: 관리자 로그인·`/admin` 접근 제한

상태: 시작 전

목표:

- Supabase Auth 기반 관리자 로그인을 구성한다.
- 서버 측에서 `/admin` 접근을 차단한다.
- 관리자 API와 공개 API 경계를 나눈다.
- RLS 정책이 관리자 전용 데이터 접근을 강제하도록 맞춘다.

완료 기준:

- 미로그인 사용자는 `/admin`에 접근할 수 없다.
- 로그인한 관리자만 문의/고객/프로젝트 데이터를 읽을 수 있다.
- 클라이언트 번들에 서비스 키나 AI API 키가 포함되지 않는다.

## 이번 주 후속 작업

- 2026-07-24: 제작 문의 폼·등록 API, 관리자 문의 목록·상세
- 2026-07-25: 고객·프로젝트 CRUD, 문의→고객·프로젝트 전환 UI
- 2026-07-26: 매출·입금 기본 관리, 1주차 전체 흐름 QA·빌드 검증

## 세션 분리 프롬프트

### 프론트엔드 세션용

```text
손 대표님의 아임웹 제작 대행 운영 웹앱 MVP 프론트엔드 세션이다. 구현 전 AGENTS.md, docs/roadmap.md, docs/current-task.md, docs/file-ownership.md를 읽어라. FSD 구조를 기준으로 `src/app`, `src/widgets`, `src/features`, `src/entities`, `src/shared`를 사용하고 `src/pages`는 만들지 마라. 직접 백엔드/RLS/마이그레이션은 수정하지 말고, 공개 사이트와 관리자 UI, 라우트/레이아웃, 폼/목록/상세 화면만 담당하라. 오늘 범위는 프로젝트 초기 세팅과 공개/관리자 레이아웃 분리이며, 7월 23일 범위는 공개 홈페이지 기본 화면이다. 변경 후 lint/type-check/build 가능한 스크립트 상태를 확인하고, 백엔드 계약이 필요한 부분은 명확한 TODO와 타입으로 남겨라.
```

### 백엔드 세션용

```text
손 대표님의 아임웹 제작 대행 운영 웹앱 MVP 백엔드 세션이다. 구현 전 AGENTS.md, docs/roadmap.md, docs/current-task.md, docs/file-ownership.md를 읽어라. FSD 구조를 기준으로 서버 계약은 `src/shared/api`, 도메인 타입/쿼리는 `src/entities`, 사용자 행동 API는 `src/features`, Route Handler는 `src/app/api`에 배치하라. UI 스타일링과 화면 레이아웃은 건드리지 말고 Supabase 스키마, Auth, RLS, 서버 API, Zod 검증, 타입/API 계약만 담당하라. 오늘 범위는 Supabase 연결과 핵심 DB 스키마 작성이며, 7월 23일 범위는 관리자 로그인과 /admin 서버 측 접근 제한이다. 비밀값은 절대 커밋하지 말고 공개 API와 관리자 API 권한 경계를 명확히 검증하라.
```
